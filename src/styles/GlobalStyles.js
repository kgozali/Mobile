'use strict';
var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({

  backIconStyleWhite: {
    fontSize: 30,
    color: 'white'
  },

  backIconStyleBlack: {
    fontSize: 30,
    color: 'Black'
  },
  viewPointStyle: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    marginLeft: 10,
    marginTop: 5
  }
});
